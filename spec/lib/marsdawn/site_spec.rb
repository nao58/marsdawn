# encoding: utf-8

require File.expand_path(File.join('../../', 'spec_helper'), File.dirname(__FILE__))

describe Marsdawn::Site do
  it 'test.' do
    #storage_config = {'type'=>'FileSystem', 'path'=>$COMPILED_DOC_DIR}
    #opts = {:key=>'kramdown', :lang=>'en', :version=>'0.0.1'}
    #opts = {:key=>'kramdown'}
    #site = Marsdawn::Site.new(opts, storage_config)
    #p site.index.under('/parser').to_html
    #p site.index.neighbor('/parser').keys
    #p site.index.to_html
    #p site.page('/parser').neighbor.to_html
  end
end
